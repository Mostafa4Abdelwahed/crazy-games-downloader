/* Explicit external Unity config resource, referenced by the game
 * document via <script src=".../fixture-config.js">. Static extraction
 * only — never executed by the importer. */
var FixtureExternalConfig = {
  dataUrl: "Build/fixture-external.data",
  frameworkUrl: "Build/fixture-external.framework.js",
  codeUrl: "Build/fixture-external.wasm",
  streamingAssetsUrl: "StreamingAssets"
};
